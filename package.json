{
  "name": "coloquio-windcss",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "eleventy --serve & npx tailwindcss -i src/style.css -c tailwind.config.js -o dist/style.css --watch",
    "deploy": "npx tailwindcss -i src/style.css -c tailwind.config.js -o dist/style.css --minify && eleventy && gh-pages -d dist -t true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "eslint": "^8.22.0",
    "tailwindcss": "^3.1.8"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^2.0.1",
    "@alpinejs/collapse": "^3.10.3",
    "alpinejs": "^3.10.3",
    "gh-pages": "^4.0.0"
  }
}
